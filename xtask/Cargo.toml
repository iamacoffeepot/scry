[package]
name = "xtask"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false

# Operational commands for the scry deployment: watch-list edits, service
# control (launchd), journal inspection. Invoked as `cargo xtask <command>`
# via the .cargo/config.toml alias.

[dependencies]
anyhow.workspace = true
clap.workspace = true
serde_json.workspace = true
